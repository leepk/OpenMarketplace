services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: openmarketplace
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - openmarketplace_pgdata:/var/lib/postgresql/data

volumes:
  openmarketplace_pgdata:
