name: Build Customer Docker

on:
  workflow_dispatch:

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create Frontend Env
        run: |
          echo "NEXT_PUBLIC_API_BASE_URL=${{ secrets.NEXT_PUBLIC_API_BASE_URL }}" > ./customer-web/.env

      - name: Build Image
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_API_BASE_URL=${{ secrets.NEXT_PUBLIC_API_BASE_URL }} \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/openmarket-customer:latest \
            ./customer-web

      - name: Push Image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/openmarket-customer:latest

      - name: Deploy To Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          port: ${{ secrets.VPS_PORT }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/openmarket-customer:latest
            docker-compose -p openmarket-customer -f /opt/openmarket/customer-compose.yml down
            docker image prune -f
            docker-compose -p openmarket-customer -f /opt/openmarket/customer-compose.yml up -d
